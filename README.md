# מדריך פריסה — Noya Nail Studio

## אופציה 1 (הכי מהירה) — Drag & Drop ל-Vercel

1. כנס ל-https://vercel.com (מחובר עם Afi1982@gmail.com)
2. לחץ **"Add New..." → "Project"**
3. גרור את התיקייה הזו (עם `index.html` ו-`vercel.json`) לאזור ההעלאה
4. תן לפרויקט שם, למשל `noya-nails`
5. לחץ **Deploy**

זהו. תוך 30 שניות תקבל URL חי כמו `noya-nails.vercel.app`.

## אופציה 2 — דרך GitHub

```bash
cd /path/to/this/folder
git init
git add .
git commit -m "Initial commit - Noya Nail Studio landing page"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/noya-nails.git
git push -u origin main
```

אז ב-Vercel: Add New → Project → Import מ-GitHub → בחר את הריפו → Deploy.

## מה כלול

- `index.html` — דף הנחיתה המלא
- `vercel.json` — הגדרות אבטחה ו-clean URLs

## ✅ מה כבר מוגדר

- WhatsApp: `054-3366077` (כפתור שולח ל-`wa.me/972543366077`)
- Instagram: `@noya2478`
- כפתור התקשרות ישיר עם הטלפון

## דומיין מותאם אישית

אחרי פריסה, ב-Vercel: Settings → Domains → הוסף דומיין משלך (למשל `noyazarfati.co.il`).
